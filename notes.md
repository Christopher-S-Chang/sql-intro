
sqlite3 baseball.sqlite3

.schema
,mode columns
.headers on
.read SQL_FILENAME
.quit

SELECT ________
FROM ______, _____
INNER JOIN _____ ON primary Key = foreign key
WHERE _____
AND ______
GROUP BY _____________
ORDER BY ____ (DESC)
LIMIT _____;

```
<>= <= >=
```